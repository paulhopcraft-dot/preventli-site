---
phase: 2
plan: 1
name: Premium calculator UI + lead capture
autonomous: true
goal_verify: ""
---

# Phase 2 — Calculator UI, lead capture, PDF

Build the public calculator page and lead capture per `agent-specs/premium-calculator.md` (§3, §6, §7). Depends on Phase 1 (the engine). Reuse the existing `app/api/contact/route.ts` pattern. Brand: navy #0A1628, green #00E676, Inter.

<tasks>

<task type="feat" name="Industry plain-English picker data + matcher">
  <files>lib/premium/industry-synonyms.ts</files>
  <action>Create lib/premium/industry-synonyms.ts: a matcher that maps a plain-English trade term (e.g. "cafe", "laundry", "plumbing") to the right WIC code from rates-2025-26.json. Export matchIndustry(query) returning ranked candidates { wic, description }. Seed a synonym map for the ~50 most common trades; fall back to substring match over the official descriptions.</action>
  <verify>file_exists: lib/premium/industry-synonyms.ts</verify>
  <done>matchIndustry returns sensible WIC candidates for common trade terms.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="IndustryPicker component">
  <files>components/premium-calculator/IndustryPicker.tsx</files>
  <action>Build a searchable combobox (Tailwind) that calls matchIndustry as the user types and shows the matched official classification for transparency. Controlled component: props value (wicCode) + onChange. Mobile-first, matches existing site styling.</action>
  <verify>npx tsc --noEmit</verify>
  <done>IndustryPicker compiles and lets a user pick a WIC by plain-English search.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="Calculator + ResultPanel components">
  <files>components/premium-calculator/Calculator.tsx
components/premium-calculator/ResultPanel.tsx</files>
  <action>Build the single-screen, live-updating calculator per spec §6: three inputs (annual wages; industry via IndustryPicker; claims cost collapsed and pre-filled with ICCR×wages); optional "last year's premium" field that upgrades capping to a true value. ResultPanel shows: estimated premium (+GST line), the savings block with the size-aware guard, the interactive "true cost of a claim" panel with the 30% capping indicator (§4b), a "how this is worked out" expander, the "email me a copy" CTA, and the canonical disclaimer + the 40% asterisk as fine print. All maths comes from lib/premium/engine.ts — no recalculation in the component.</action>
  <verify>npx tsc --noEmit</verify>
  <done>Calculator + ResultPanel compile and render premium, savings, and the capped claim-impact, all from the engine.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="Calculator page route">
  <files>app/premium-calculator/page.tsx</files>
  <action>Create the /premium-calculator page (App Router) rendering the Calculator, with site nav/footer, SEO metadata targeting "WorkCover premium calculator Victoria". Mobile-first.</action>
  <verify>npm run build</verify>
  <done>The site builds and /premium-calculator renders the calculator.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="Branded PDF report">
  <files>lib/premium/pdf/PremiumReport.tsx
package.json</files>
  <action>Add @react-pdf/renderer (npm install @react-pdf/renderer) and build PremiumReport.tsx: a branded one-page PDF of the estimate — inputs, premium, savings scenario, the claim-impact, the size-aware caveat, the canonical disclaimer + 40% asterisk, and lisah@preventli.ai contact. Renders server-side to a buffer.</action>
  <verify>npx tsc --noEmit</verify>
  <done>PremiumReport renders a branded PDF buffer from a result object.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="Lead capture API route">
  <files>app/api/premium-lead/route.ts</files>
  <action>Create POST /api/premium-lead mirroring app/api/contact/route.ts exactly: validate { email, wages, wicCode, claimsCost, premium, saving }; insert into Supabase table premium_calc_leads (status "new"); send two Resend emails (prospect gets the branded result + PDF attachment from PremiumReport; team notification to paul.hopcraft@gmail.com). From noreply@preventli.ai. Never fail the request on email error. NOTE in a code comment that the premium_calc_leads table must exist in Supabase (mirror contact_submissions).</action>
  <verify>npx tsc --noEmit</verify>
  <done>POST /api/premium-lead stores a lead and emails the PDF, following the contact-route pattern.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="checkpoint:human-verify" name="Human sign-off: look, test email, copy">
  <action>PAUSE for Paul. The machine-checkable parts are done (builds, types, lead route). These need human eyes per spec §8a: (1) does the page look good / on-brand; (2) submit a real test email and confirm the branded PDF arrives correctly; (3) sign off the savings copy and the 40% asterisk wording. Also: create the premium_calc_leads table in Supabase before going live.</action>
  <done>Paul has reviewed the look, received a correct test PDF, and approved the copy.</done>
</task>

</tasks>
