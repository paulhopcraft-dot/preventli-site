---
phase: 1
plan: 2
name: Premium calculator UI + lead capture
autonomous: true
goal_verify: ""
---

# Phase 1 / Plan 2 — Calculator UI, lead capture, PDF

Runs AFTER plan 01-01 (the engine) in the same Ralph phase, so `-Phase 1 -Loop` builds the
whole calculator in one goal. Build the public page + lead capture per
`agent-specs/premium-calculator.md` (§3, §6, §7). Reuse the existing `app/api/contact/route.ts`
pattern. Brand: navy #0A1628, green #00E676, Inter. The phase finish-line (`goal_verify`) lives
on plan 01-01 and covers the FULL build (npm run build + all vitest + oracle markers).

<tasks>

<task type="feat" name="Industry plain-English picker data + matcher">
  <files>lib/premium/industry-synonyms.ts</files>
  <action>Create lib/premium/industry-synonyms.ts: a matcher that maps a plain-English trade term (e.g. "cafe", "laundry", "plumbing") to the right WIC code from rates-2025-26.json. Export matchIndustry(query) returning ranked candidates { wic, description }. Seed a synonym map for the ~50 most common trades; fall back to substring match over the official descriptions.</action>
  <verify>file_exists: lib/premium/industry-synonyms.ts</verify>
  <done>matchIndustry returns sensible WIC candidates for common trade terms.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="IndustryPicker component">
  <files>components/premium-calculator/IndustryPicker.tsx</files>
  <action>Build a searchable combobox (Tailwind) that calls matchIndustry as the user types and shows the matched official classification for transparency. Controlled component: props value (wicCode) + onChange. Mobile-first, matches existing site styling.</action>
  <verify>npx tsc --noEmit</verify>
  <done>IndustryPicker compiles and lets a user pick a WIC by plain-English search.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="Calculator + ResultPanel components">
  <files>components/premium-calculator/Calculator.tsx
components/premium-calculator/ResultPanel.tsx</files>
  <action>Build the single-screen, live-updating calculator per spec §6: three inputs (annual wages; industry via IndustryPicker; claims cost collapsed and pre-filled with ICCR×wages); optional "last year's premium" field that upgrades capping to a true value. ResultPanel shows: estimated premium (+GST line), the savings block with the size-aware guard, the interactive "true cost of a claim" panel with the 30% capping indicator (§4b), a "how this is worked out" expander, the "email me a copy" CTA, and the canonical disclaimer + the 40% asterisk as fine print. CRITICAL COPY RULES (spec §11): the headline must say "lower CLAIMS COST" never "lower premium"; the 40% is a claims-cost reduction; small employers (rem ≤ 200000) must show the size-aware guard message and NO saving figure. All maths comes from lib/premium/engine.ts — no recalculation in the component.</action>
  <verify>npx tsc --noEmit</verify>
  <done>Calculator + ResultPanel compile and render premium, savings, and the capped claim-impact, all from the engine.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="Calculator page route">
  <files>app/premium-calculator/page.tsx</files>
  <action>Create the /premium-calculator page (App Router) rendering the Calculator, with site nav/footer, SEO metadata targeting "WorkCover premium calculator Victoria". Mobile-first. The canonical disclaimer text "For illustration purposes only" MUST appear in the rendered output (it is a §8a gate).</action>
  <verify>npm run build</verify>
  <done>The site builds and /premium-calculator renders the calculator + disclaimer.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="Branded PDF report">
  <files>lib/premium/pdf/PremiumReport.tsx
package.json</files>
  <action>Add @react-pdf/renderer (npm install @react-pdf/renderer) and build PremiumReport.tsx: a branded one-page PDF of the estimate — inputs, premium, savings scenario, the claim-impact, the size-aware caveat, the canonical disclaimer + 40% asterisk, and lisah@preventli.ai contact. Renders server-side to a buffer.</action>
  <verify>npx tsc --noEmit</verify>
  <done>PremiumReport renders a branded PDF buffer from a result object.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="Lead capture API route">
  <files>app/api/premium-lead/route.ts</files>
  <action>Create POST /api/premium-lead mirroring app/api/contact/route.ts exactly: validate { email, wages, wicCode, claimsCost, premium, saving }; insert into Supabase table premium_calc_leads (status "new"); send two Resend emails (prospect gets the branded result + PDF attachment from PremiumReport; team notification to paul.hopcraft@gmail.com). From noreply@preventli.ai. Never fail the request on email error. NOTE in a code comment that the premium_calc_leads table must exist in Supabase (mirror contact_submissions).</action>
  <verify>npx tsc --noEmit</verify>
  <done>POST /api/premium-lead stores a lead and emails the PDF, following the contact-route pattern.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="test" name="Lead route test (mocked Supabase + Resend)">
  <files>app/api/premium-lead/route.test.ts
vitest.config.ts</files>
  <action>Write a vitest test for POST /api/premium-lead that mocks the Supabase client and Resend (vi.mock) and asserts: a valid payload returns ok, inserts one premium_calc_leads row, and invokes the Resend send path (assert it was called); an email error does NOT fail the request. Call the route handler directly (no running server). ALSO update vitest.config.ts so test.include covers both "lib/**/*.test.ts" and "app/**/*.test.ts" (so `npx vitest run` picks this up). Do NOT hit the network.</action>
  <verify>npx vitest run app/api/premium-lead</verify>
  <done>The route test passes with Supabase + Resend mocked; vitest discovers app/ tests.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="checkpoint:human-verify" name="Push, preview, sign-off, merge (DEPLOY GATE)">
  <action>PAUSE for Paul. The machine-checkable build is green (goal_verify: npm run build + all vitest + oracle markers). These need a human per spec §8a — and per Paul's merge policy preventli-site is the public live site, so the merge is HIS click, not the bot's:
  1. Push the feature branch and open a PR (gh pr create) — Vercel auto-builds a PREVIEW URL off the PR.
  2. On the preview: confirm the page looks good / on-brand; a real input renders a sensible premium; a small-employer (<$200k) input shows the size-aware guard message and NO saving figure; the disclaimer + 40% asterisk read correctly and say "lower claims cost" (never "lower premium").
  3. Submit a real test lead and confirm the branded PDF arrives correctly.
  4. Create the premium_calc_leads table in Supabase (mirror contact_submissions) BEFORE going live.
  5. Sign off the savings copy + the 40% asterisk wording.
  6. MERGE the PR -> Vercel deploys to preventli.ai/premium-calculator. (This merge is the deploy; Paul performs it.)</action>
  <done>Paul reviewed the preview, got a correct test PDF, created the Supabase table, approved the copy, and merged (deployed).</done>
</task>

</tasks>
