---
phase: 1
plan: 1
name: Premium engine — deterministic calc + golden tests
autonomous: true
goal_verify: npm run build && npx vitest run && findstr /c:"1.042814" lib\premium\engine.test.ts && findstr /c:"368274.99" lib\premium\engine.test.ts
---

# Phase 1 — Premium calculation engine

Build the deterministic WorkCover VIC premium engine per `agent-specs/premium-calculator.md` (§4 and §4b). Pure TypeScript, no AI in the maths. Each task is atomic and runs in a fresh context — the spec is injected where needed.

<tasks>

<task type="chore" name="Add vitest test runner">
  <files>package.json</files>
  <action>Add vitest as a devDependency and a "test" script set to "vitest run" by running `npm install -D vitest`. If tests under lib/ need a config to be discovered, add a minimal vitest.config.ts (test.include = ["lib/**/*.test.ts"]). Do NOT change, add, or upgrade any other dependency.</action>
  <verify>npx vitest --version</verify>
  <done>vitest is installed and `npm test` runs it.</done>
</task>

<task type="feat" name="Add 2025-26 industry rates data">
  <files>lib/premium/rates-2025-26.json</files>
  <action>Create lib/premium/rates-2025-26.json with the official 2025-26 WorkSafe industry rates by copying the full contents of D:\Downloads\ws_industry_rates_2025-26.json verbatim (529 industries; each entry has wic, description, iccr_2025_26, industry_rate_2025_26). Do not edit the values.</action>
  <verify>file_exists: lib/premium/rates-2025-26.json</verify>
  <done>The rates file exists with all 529 industries.</done>
</task>

<task type="feat" name="Add 2025-26 premium parameters">
  <files>lib/premium/params-2025-26.ts</files>
  <action>Create lib/premium/params-2025-26.ts exporting the WorkSafe 2025-26 constants as a typed object: K = 2_000_000, maxWeight = 1, smallEmployerThreshold = 200_000, minPremium = 330, gstRate = 0.10, cappingRate = 0.30, minCappingRatio = 0.25, buyoutRate = 0.10, deductible = 15_500, schemeAverageRate = 0.018, financialYear = "2025-26". Values are documented in the spec §4.</action>
  <verify>file_exists: lib/premium/params-2025-26.ts</verify>
  <done>params-2025-26.ts exports the constants above with explicit types.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="feat" name="Build the deterministic premium engine">
  <files>lib/premium/engine.ts</files>
  <action>Implement the pure deterministic premium engine exactly per the VALIDATED chain in agent-specs/premium-calculator.md §4 and §4b (EPR = ROUND(1 + Z*(PI-1)*T/1096, 6); Z = ROUND(X/(X+K),6); X = ir*expRem; PI = ROUND(ECCR/ICCR,6)). The Python reference implementation is .planning/phases/01-premium-engine/decode/04_verify_princes.py and 05_prove_savings.py — port it, matching its rounding (ROUND = half-away-from-zero via the decimal approach, NOT JS Math.round). Export with explicit return types: (0) CORE computeEpr({ claimsWindow, expRem, X, iccr, K, maxWeight, T }) returning { epr, pi, z } — this is what the Tier-1 golden cases (G1/G2) assert to the dollar; (1) lookupIndustry(wicCode) returning { ir, iccr, description }; (2) computePremium({ remuneration, wicCode, claimsCost? }) — the public WRAPPER: derives expRem = 3*remuneration and X = ir*expRem, applies the small-employer override (remuneration <= 200000 -> EPR = 1) and the minimum premium, calls computeEpr, returns { premium, gst, epr, pi, z, smallEmployer }; (3) savingsScenario({ remuneration, wicCode, claimsCost? }) returning baseline vs 40%-lower-CLAIMS-COST premium and the saving, with the size-aware $0 case for small employers (NEVER label it "40% off premium"); (4) claimImpact({ remuneration, wicCode, claimCost, priorYearPremium? }) returning the new premium with the 30% annual-rise cap applied, a capped flag, the uncapped figure, and the ~3-year total (3-yr premium impact = Z*(IR/ICCR)*claimCost). All functions pure — no I/O, no network. Read constants from params-2025-26.ts and rates from rates-2025-26.json.</action>
  <verify>npx tsc --noEmit</verify>
  <done>engine.ts compiles and exports the four functions per spec.</done>
  <context>agent-specs/premium-calculator.md</context>
</task>

<task type="test" name="Golden tests vs WorkSafe simulator">
  <files>lib/premium/engine.test.ts</files>
  <action>Write vitest golden tests for engine.ts that assert against the POPULATED "Golden cases" table in agent-specs/premium-calculator.md §8 (validated 2026-06-24). MUST include the Tier-1 exact cases: G1 -> EPR exactly 1.042814, and G2 -> premium exactly 368274.99 (these are the DoD oracle markers). Also cover Tier-2: G3 savings (~336600/yr), G4 small-employer guard (EPR=1, saving=0), G5 claim impact (+34949/yr, +104846/3yr). The Python reference (with the exact inputs + expected values) is .planning/phases/01-premium-engine/decode/04_verify_princes.py and 05_prove_savings.py — port those assertions. CRITICAL: assert the oracle numbers from the spec/decode — do NOT invent expected values. Use the same rounding mode as the engine.</action>
  <verify>npx vitest run lib/premium</verify>
  <done>Golden tests pass; test file asserts EPR 1.042814 and premium 368274.99 (the DoD markers).</done>
  <context>agent-specs/premium-calculator.md</context>
  <context>.planning/phases/01-premium-engine/decode/04_verify_princes.py</context>
  <context>.planning/phases/01-premium-engine/decode/05_prove_savings.py</context>
</task>

</tasks>
